<!DOCTYPE html>
<html>
    <head> 
        <title>
            RateMyPark
        </title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
        <link rel="stylesheet" href="/stylesheets/main.css">
        <!-- Mapbox API links -->
        <script src='https://api.mapbox.com/mapbox-gl-js/v1.11.0/mapbox-gl.js'></script>
        <link href='https://api.mapbox.com/mapbox-gl-js/v1.11.0/mapbox-gl.css' rel='stylesheet'/>
    </head>
    <body>
    
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
                <div class="navbar-header">
                    <a href="/" class="navbar-brand">RateMyPark</a>
                </div>
                <div class="nav-item">
                    <a class="nav-link tabs" href="/parks">Home</a>
                </div>
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav ml-auto">
                        <% if (!currentUser) { %>
                            <li class="nav-item"><a class="nav-link tabs" href="/login">Login</a></li>
                            <li class="nav-item"><a class="nav-link tabs" href="/register">Signup</a></li>
                        <% } else { %>
                            <li class="nav-item"><a class="nav-link tabs" href="#">Signed In as: <%= currentUser.username %></a></li>
                            <li class="nav-item"><a class="nav-link tabs" href="/logout">Logout</a></li>
                        <% } %> 
                    </ul>
                </div>
            </div>
        </nav>

        <div class="container">
            <% if (error && error.length > 0) { %>
                <div class="alert alert-danger alerts" role="alert">
                    <%= error %>
                </div>
            <% } %>
            <% if (success && success.length > 0) { %>
                <div class="alert alert-success alerts" role="alert">
                    <%= success %>
                </div>
            <% } %>
        </div>